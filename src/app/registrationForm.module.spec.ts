import { RegistrationFormModule } from './registrationForm.module';

describe('module: RegistrationFormModule', () => {
  it('should be defined', () => {
    expect(RegistrationFormModule).toBeDefined();
  });
});
